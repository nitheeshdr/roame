# Base URL of the Roame API (the Next.js app). Use your machine's LAN IP for a
# physical device, e.g. http://192.168.1.20:3000
EXPO_PUBLIC_API_URL="http://localhost:3000"
# Google OAuth Web client id (same one used by the web app). Real sign-in only.
EXPO_PUBLIC_GOOGLE_CLIENT_ID=""
